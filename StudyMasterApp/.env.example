# API Configuration
API_URL=http://localhost:5000/api
NODE_ENV=development

# JWT (for production - in dev we use mock auth)
JWT_SECRET=your-super-secret-key-change-in-production

# Optional: Analytics keys
# SENTRY_DSN=
# MIXPANEL_TOKEN=
